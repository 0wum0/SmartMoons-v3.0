{% include 'overall_header.twig' %}

<div class="dash-header">
    <h1><span style="color: var(--adm-accent);">&#129302;</span> Bot Manager</h1>
    <p>Verwaltung der KI-Spieler, Ressourcen-Kontingente und Strategien (PVE + PVP)</p>
</div>

<div class="period-tabs" style="margin-bottom: 25px;">
    <a href="admin.php?page=EditBots" class="period-tab {% if mode == 'default' %}active{% endif %}">Kontingente</a>
    <a href="admin.php?page=EditBots&mode=show_all_bots" class="period-tab {% if mode == 'show_all_bots' %}active{% endif %}">Schiffsliste</a>
    <a href="admin.php?page=EditBots&mode=show_edit_bot_types" class="period-tab {% if mode == 'show_edit_bot_types' %}active{% endif %}">Strategien / Typen</a>
    <a href="admin.php?page=EditBots&mode=bot_settings" class="period-tab {% if mode == 'bot_settings' %}active{% endif %}">⚙️ Bot-Einstellungen</a>
    <a href="admin.php?page=EditBots&mode=ai_behavior" class="period-tab {% if mode == 'ai_behavior' %}active{% endif %}">🤖 KI-Verhalten</a>
    <a href="admin.php?page=EditBots&mode=live_monitor" class="period-tab {% if mode == 'live_monitor' %}active{% endif %}">📊 Live-Monitor</a>
    <a href="admin.php?page=EditBots&mode=analytics" class="period-tab {% if mode == 'analytics' %}active{% endif %}">📈 Analytics</a>
    <a href="admin.php?page=EditBots&mode=dev_tools" class="period-tab {% if mode == 'dev_tools' %}active{% endif %}">🛠️ Dev-Tools</a>
    <a href="admin.php?page=EditBots&mode=show_edit_bot_users" class="period-tab {% if mode == 'show_edit_bot_users' %}active{% endif %}">Bot-Accounts</a>
    <a href="admin.php?page=EditBots&mode=show_edit_bot_planets" class="period-tab {% if mode == 'show_edit_bot_planets' %}active{% endif %}">Bot-Planeten</a>
    <a href="admin.php?page=EditBots&mode=show_create_bot" class="period-tab {% if mode == 'show_create_bot' or mode == 'create_bot' %}active{% endif %}">Generator</a>
</div>

{% if message %}
<div class="report-card" style="margin-bottom: 20px; border-left: 4px solid {% if message.class == 'success' %}var(--adm-success){% else %}var(--adm-danger){% endif %}; padding: 15px;">
    <span style="color: var(--adm-text); font-weight: 500;">{{ message.text }}</span>
</div>
{% endif %}

{# ==================== BOT SETTINGS TAB ==================== #}
{% if mode == 'bot_settings' %}
<div class="report-card">
    <div class="report-card-title">⚙️ Globale Bot-Einstellungen</div>

    <form method="post" action="admin.php?page=EditBots&mode=bot_settings">
        <input type="hidden" name="save_bot_settings" value="1">

        <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(400px,1fr));gap:20px;">

            <div class="config-section">
                <h4><span style="color: var(--adm-accent);">⚡</span> Parallelität & Performance</h4>

                <div class="form-group">
                    <label>Anzahl paralleler Bots pro Cronjob-Lauf</label>
                    <input type="number" name="bots_per_tick" value="{{ data.bot_config.bots_per_tick|default(5) }}" min="1" max="50" class="glass-input">
                    <small>Wie viele Bots sollen <b>gleichzeitig</b> aktiv sein? (1-50)<br>
                    <b>Standard: 5</b> | Mehr = schneller aber mehr Server-Last</small>
                </div>

                <div class="form-group">
                    <label>Cronjob-Intervall (Sekunden)</label>
                    <input type="number" name="cron_interval" value="{{ data.bot_config.cron_interval|default(60) }}" min="30" max="300" class="glass-input" readonly>
                    <small>Wird in der Cronjob-Konfiguration eingestellt (Standard: 60s)</small>
                </div>

                <div class="form-group">
                    <label>Max. Aktionen pro Bot-Tick</label>
                    <input type="number" name="max_actions_per_tick" value="{{ data.bot_config.max_actions_per_tick|default(3) }}" min="1" max="10" class="glass-input">
                    <small>Wie viele Aktionen darf ein Bot pro Tick machen?<br>
                    (Bauen + Forschen + Flotten = max. X Aktionen)</small>
                </div>
            </div>

            <div class="config-section">
                <h4><span style="color: var(--adm-neon-green);">🕐</span> Timing & Delays</h4>

                <div class="form-group">
                    <label>Min. Delay zwischen Bot-Ticks (Sekunden)</label>
                    <input type="number" name="min_tick_delay" value="{{ data.bot_config.min_tick_delay|default(1800) }}" min="300" max="7200" class="glass-input">
                    <small>Mindestens X Sekunden zwischen zwei Aktionen desselben Bots<br>
                    <b>Standard: 1800s (30 Min)</b></small>
                </div>

                <div class="form-group">
                    <label>Max. Delay zwischen Bot-Ticks (Sekunden)</label>
                    <input type="number" name="max_tick_delay" value="{{ data.bot_config.max_tick_delay|default(7200) }}" min="1800" max="14400" class="glass-input">
                    <small>Maximal X Sekunden zwischen zwei Aktionen desselben Bots<br>
                    <b>Standard: 7200s (2 Std)</b></small>
                </div>

                <div class="form-group">
                    <label>Crash-Delay (Sekunden)</label>
                    <input type="number" name="crash_delay" value="{{ data.bot_config.crash_delay|default(1800) }}" min="300" max="3600" class="glass-input">
                    <small>Bei Bot-Crash: Nächster Versuch erst nach X Sekunden<br>
                    <b>Standard: 1800s (30 Min)</b></small>
                </div>
            </div>

            <div class="config-section">
                <h4><span style="color: var(--adm-warning);">🎯</span> Aktivitätsmodus</h4>

                <div class="checkbox-group">
                    <label class="chk">
                        <input type="checkbox" name="random_timing" {% if data.bot_config.random_timing %}checked{% endif %}>
                        Zufällige Timing-Variation (+/- 20%)
                    </label>
                    <small style="margin-left: 30px; color: var(--adm-text-muted); font-size: 11px;">
                        Bots ticken nicht exakt nach Schema → wirkt menschlicher
                    </small>
                </div>

                <div class="checkbox-group" style="margin-top: 15px;">
                    <label class="chk">
                        <input type="checkbox" name="spread_bots" {% if data.bot_config.spread_bots|default(true) %}checked{% endif %}>
                        Bots zeitlich verteilen (Spread)
                    </label>
                    <small style="margin-left: 30px; color: var(--adm-text-muted); font-size: 11px;">
                        Nicht alle Bots auf einmal starten → reduziert Last-Spitzen
                    </small>
                </div>
            </div>

        </div>

        <div style="margin-top: 20px; text-align: right;">
            <button type="submit" class="btn-save">💾 Einstellungen Speichern</button>
        </div>
    </form>
</div>
{% endif %}

{# ==================== AI BEHAVIOR TAB ==================== #}
{% if mode == 'ai_behavior' %}
<div class="report-card">
    <div class="report-card-title">🤖 Bot KI-Verhalten Konfiguration</div>
    
    <div style="display: flex; gap: 10px; flex-wrap: wrap; margin-bottom: 20px;">
        {% for type in data.botTypes %}
        <button type="button"
                onclick="$('.ai-config-form').hide(); $('#ai_{{ type.id }}').fadeIn();"
                class="type-select-btn">
            {{ type.name }}
        </button>
        {% endfor %}
    </div>

    {% for type in data.botTypes %}
    <form method="post" action="admin.php?page=EditBots&mode=ai_behavior" id="ai_{{ type.id }}" class="ai-config-form" style="display: {% if loop.first %}block{% else %}none{% endif %};">
        <input type="hidden" name="save_ai_behavior" value="1">
        <input type="hidden" name="type_id" value="{{ type.id }}">

        <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(350px,1fr));gap:20px;">

            {# ECONOMY PRIORITY #}
            <div class="config-section">
                <h4><span style="color: var(--adm-neon-green);">💰</span> Wirtschafts-Priorität</h4>
                
                <div class="form-group">
                    <label>Bau-Strategie</label>
                    <select name="build_strategy" class="glass-input">
                        <option value="balanced" {% if type.build_strategy == 'balanced' %}selected{% endif %}>Ausgewogen (Mine→Kraftwerk→Speicher)</option>
                        <option value="metal_rush" {% if type.build_strategy == 'metal_rush' %}selected{% endif %}>Metall-Rush (Metallmine Priorität)</option>
                        <option value="crystal_rush" {% if type.build_strategy == 'crystal_rush' %}selected{% endif %}>Kristall-Rush (Kristallmine Priorität)</option>
                        <option value="energy_first" {% if type.build_strategy == 'energy_first' %}selected{% endif %}>Energie Zuerst (Solar/Fusion)</option>
                        <option value="infrastructure" {% if type.build_strategy == 'infrastructure' %}selected{% endif %}>Infrastruktur (Labor/Werft)</option>
                    </select>
                </div>

                <div class="form-group">
                    <label>Forschungs-Strategie</label>
                    <select name="research_strategy" class="glass-input">
                        <option value="economy" {% if type.research_strategy == 'economy' %}selected{% endif %}>Wirtschaft (Energie/Plasma/Astro)</option>
                        <option value="military" {% if type.research_strategy == 'military' %}selected{% endif %}>Militär (Waffen/Schilde/Panzerung)</option>
                        <option value="drive" {% if type.research_strategy == 'drive' %}selected{% endif %}>Antriebstech (Geschwindigkeit)</option>
                        <option value="balanced" {% if type.research_strategy == 'balanced' %}selected{% endif %}>Ausgewogener Mix</option>
                    </select>
                </div>

                <div class="form-group">
                    <label>Werft-Fokus</label>
                    <select name="shipyard_focus" class="glass-input">
                        <option value="cargo" {% if type.shipyard_focus == 'cargo' %}selected{% endif %}>Transporter (Transport)</option>
                        <option value="recycler" {% if type.shipyard_focus == 'recycler' %}selected{% endif %}>Recycler (Trümmerfeld)</option>
                        <option value="fighter" {% if type.shipyard_focus == 'fighter' %}selected{% endif %}>Jäger (Kampf)</option>
                        <option value="mixed_fleet" {% if type.shipyard_focus == 'mixed_fleet' %}selected{% endif %}>Gemischte Flotte</option>
                    </select>
                </div>

                <div class="form-group">
                    <label>Min. Ressourcen vor Upgrade</label>
                    <input type="number" name="min_resources_for_upgrade" value="{{ type.min_resources_for_upgrade|default(5000) }}" class="glass-input">
                    <small>Wartet bis genug Ressourcen vorhanden sind</small>
                </div>
            </div>

            {# FLEET BEHAVIOR #}
            <div class="config-section">
                <h4><span style="color: var(--adm-accent);">🚀</span> Flotten-Verhalten</h4>

                <div class="form-group">
                    <label>Expeditions-Frequenz</label>
                    <select name="expedition_frequency" class="glass-input">
                        <option value="never" {% if type.expedition_frequency == 'never' %}selected{% endif %}>Niemals</option>
                        <option value="when_idle" {% if type.expedition_frequency == 'when_idle' %}selected{% endif %}>Wenn untätig (Standard)</option>
                        <option value="aggressive" {% if type.expedition_frequency == 'aggressive' %}selected{% endif %}>Aggressiv (Alle Slots)</option>
                    </select>
                </div>

                <div class="form-group">
                    <label>Auto-Recycling eigener TF</label>
                    <select name="auto_recycle" class="glass-input">
                        <option value="always" {% if type.auto_recycle == 'always' %}selected{% endif %}>Immer (Sofort)</option>
                        <option value="when_worth" {% if type.auto_recycle == 'when_worth' %}selected{% endif %}>Wenn lohnenswert (>10k)</option>
                        <option value="never" {% if type.auto_recycle == 'never' %}selected{% endif %}>Niemals</option>
                    </select>
                </div>

                <div class="form-group">
                    <label>Flottengröße pro Mission</label>
                    <input type="number" name="fleet_percentage" value="{{ type.fleet_percentage|default(50) }}" min="10" max="100" class="glass-input">
                    <small>% der Flotte pro Mission (10-100%)</small>
                </div>

                <div class="form-group">
                    <label>Bevorzugte Missions-Distanz</label>
                    <input type="number" name="max_mission_distance" value="{{ type.max_mission_distance|default(100) }}" class="glass-input">
                    <small>Max. Systeme Entfernung</small>
                </div>
            </div>

            {# ADVANCED AI #}
            <div class="config-section">
                <h4><span style="color: var(--adm-neon-purple);">⚙️</span> Erweiterte KI</h4>

                <div class="form-group">
                    <label>Entscheidungsgeschwindigkeit</label>
                    <select name="decision_speed" class="glass-input">
                        <option value="slow" {% if type.decision_speed == 'slow' %}selected{% endif %}>Langsam (Konservativ)</option>
                        <option value="normal" {% if type.decision_speed == 'normal' %}selected{% endif %}>Normal</option>
                        <option value="fast" {% if type.decision_speed == 'fast' %}selected{% endif %}>Schnell (Aggressiv)</option>
                    </select>
                </div>

                <div class="form-group">
                    <label>Risiko-Toleranz</label>
                    <input type="range" name="risk_tolerance" min="0" max="100" value="{{ type.risk_tolerance|default(30) }}" class="range-slider" oninput="this.nextElementSibling.textContent = this.value + '%'">
                    <output>{{ type.risk_tolerance|default(30) }}%</output>
                    <small>Wie aggressiv bei Raids/Expos</small>
                </div>

                <div class="form-group">
                    <label>Ressourcen-Priorität</label>
                    <div style="display:flex;gap:10px;">
                        <input type="number" name="priority_metal" value="{{ type.priority_metal|default(1.0) }}" step="0.1" class="glass-input" placeholder="Metall">
                        <input type="number" name="priority_crystal" value="{{ type.priority_crystal|default(0.7) }}" step="0.1" class="glass-input" placeholder="Kristall">
                        <input type="number" name="priority_deut" value="{{ type.priority_deut|default(0.5) }}" step="0.1" class="glass-input" placeholder="Deut">
                    </div>
                    <small>Gewichtung beim Ressourcen-Management</small>
                </div>

                <div class="checkbox-group">
                    <label class="chk">
                        <input type="checkbox" name="auto_research" {% if type.auto_research %}checked{% endif %}>
                        Auto-Forschung (Automatische Tech-Warteschlange)
                    </label>
                    <label class="chk">
                        <input type="checkbox" name="smart_fleet_save" {% if type.smart_fleet_save %}checked{% endif %}>
                        Smart Fleet Save (Bei Gefahr zurückrufen)
                    </label>
                    <label class="chk">
                        <input type="checkbox" name="opportunistic_raids" {% if type.opportunistic_raids %}checked{% endif %}>
                        Opportunistische Raids (Schwache Ziele nutzen)
                    </label>
                </div>
            </div>

        </div>

        <div style="margin-top: 20px; text-align: right;">
            <button type="submit" class="btn-save">💾 KI-Konfiguration Speichern</button>
        </div>
    </form>
    {% endfor %}
</div>
{% endif %}

{# ==================== LIVE MONITOR TAB ==================== #}
{% if mode == 'live_monitor' %}
<div class="report-card">
    <div class="report-card-title">📊 Live Bot-Monitor <span id="live-status" style="color: var(--adm-success); margin-left: 10px;">●</span></div>
    
    <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 15px; margin-bottom: 20px;">
        <div class="stat-box">
            <div class="stat-label">Aktive Bots</div>
            <div class="stat-value" id="stat-active">{{ data.stats.active_bots|default(0) }}</div>
        </div>
        <div class="stat-box">
            <div class="stat-label">Bau-Warteschlange</div>
            <div class="stat-value" id="stat-building">{{ data.stats.building_queue|default(0) }}</div>
        </div>
        <div class="stat-box">
            <div class="stat-label">Forschungs-Warteschlange</div>
            <div class="stat-value" id="stat-research">{{ data.stats.research_queue|default(0) }}</div>
        </div>
        <div class="stat-box">
            <div class="stat-label">Flotten im Weltall</div>
            <div class="stat-value" id="stat-fleets">{{ data.stats.fleets_flying|default(0) }}</div>
        </div>
    </div>

    <div id="live-activity-feed" style="background: rgba(0,0,0,0.2); border-radius: 8px; padding: 15px; max-height: 500px; overflow-y: auto; font-family: 'Courier New', monospace; font-size: 12px;">
        <div style="color: var(--adm-text-muted);">Warte auf Bot-Aktivität...</div>
    </div>
</div>

<script>
let lastLogTimestamp = 0;

function fetchLiveData() {
    fetch('admin.php?page=EditBots&mode=live_monitor&ajax=1&last=' + lastLogTimestamp)
        .then(r => r.json())
        .then(data => {
            // Update stats
            if(data.stats) {
                document.getElementById('stat-active').textContent = data.stats.active_bots || 0;
                document.getElementById('stat-building').textContent = data.stats.building_queue || 0;
                document.getElementById('stat-research').textContent = data.stats.research_queue || 0;
                document.getElementById('stat-fleets').textContent = data.stats.fleets_flying || 0;
            }

            // Update activity feed
            if(data.logs && data.logs.length > 0) {
                const feed = document.getElementById('live-activity-feed');
                data.logs.forEach(log => {
                    const line = document.createElement('div');
                    line.style.marginBottom = '5px';
                    line.style.color = log.type === 'error' ? 'var(--adm-danger)' : 
                                      log.type === 'success' ? 'var(--adm-success)' : 
                                      'var(--adm-text)';
                    line.textContent = `[${log.time}] ${log.message}`;
                    feed.insertBefore(line, feed.firstChild);
                });
                lastLogTimestamp = data.last_timestamp;
            }

            document.getElementById('live-status').style.color = 'var(--adm-success)';
        })
        .catch(err => {
            document.getElementById('live-status').style.color = 'var(--adm-danger)';
        });
}

// Poll every 5 seconds
setInterval(fetchLiveData, 5000);
fetchLiveData();
</script>
{% endif %}

{# ==================== ANALYTICS TAB ==================== #}
{% if mode == 'analytics' %}
<div class="report-card">
    <div class="report-card-title">📈 Bot Analytics & Performance</div>

    {# KPI Cards #}
    <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 15px; margin-bottom: 30px;">
        <div class="kpi-card">
            <div class="kpi-icon">⚡</div>
            <div class="kpi-value">{{ data.analytics.total_actions|number_format }}</div>
            <div class="kpi-label">Gesamt-Aktionen (24h)</div>
        </div>
        <div class="kpi-card">
            <div class="kpi-icon">💰</div>
            <div class="kpi-value">{{ data.analytics.resources_per_hour|number_format }}</div>
            <div class="kpi-label">Ressourcen/Stunde</div>
        </div>
        <div class="kpi-card">
            <div class="kpi-icon">🎯</div>
            <div class="kpi-value">{{ data.analytics.success_rate }}%</div>
            <div class="kpi-label">Erfolgsquote</div>
        </div>
        <div class="kpi-card">
            <div class="kpi-icon">🚀</div>
            <div class="kpi-value">{{ data.analytics.fleets_active|number_format }}</div>
            <div class="kpi-label">Aktive Flotten</div>
        </div>
    </div>

    {# Aktivitäts-Heatmap #}
    <div style="background: rgba(0,0,0,0.2); border-radius: 12px; padding: 20px; margin-bottom: 20px;">
        <h3 style="color: var(--adm-accent); margin-top: 0;">🔥 Aktivitäts-Heatmap (24 Stunden)</h3>
        <div style="position: relative; height: 200px;">
            <canvas id="activityHeatmap"></canvas>
        </div>
    </div>

    {# Ressourcen-Flow Chart #}
    <div style="background: rgba(0,0,0,0.2); border-radius: 12px; padding: 20px; margin-bottom: 20px;">
        <h3 style="color: var(--adm-neon-green); margin-top: 0;">💎 Ressourcen-Flow (Letzte 24h)</h3>
        <div style="position: relative; height: 300px;">
            <canvas id="resourcesChart"></canvas>
        </div>
    </div>

    {# Erfolgsquoten #}
    <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 20px; margin-bottom: 20px;">
        
        <div style="background: rgba(0,0,0,0.2); border-radius: 12px; padding: 20px;">
            <h4 style="color: var(--adm-accent); margin-top: 0;">🎯 Raid Erfolgsquote</h4>
            <div style="display: flex; align-items: center; gap: 15px;">
                <div class="progress-circle" data-percent="{{ data.analytics.raid_success_rate }}">
                    <span>{{ data.analytics.raid_success_rate }}%</span>
                </div>
                <div>
                    <div style="color: var(--adm-text-muted); font-size: 12px;">Erfolgreich: {{ data.analytics.raids_success }}</div>
                    <div style="color: var(--adm-text-muted); font-size: 12px;">Gesamt: {{ data.analytics.raids_total }}</div>
                </div>
            </div>
        </div>

        <div style="background: rgba(0,0,0,0.2); border-radius: 12px; padding: 20px;">
            <h4 style="color: var(--adm-neon-purple); margin-top: 0;">🌌 Expeditions Erfolgsquote</h4>
            <div style="display: flex; align-items: center; gap: 15px;">
                <div class="progress-circle" data-percent="{{ data.analytics.expo_success_rate }}">
                    <span>{{ data.analytics.expo_success_rate }}%</span>
                </div>
                <div>
                    <div style="color: var(--adm-text-muted); font-size: 12px;">Erfolgreich: {{ data.analytics.expos_success }}</div>
                    <div style="color: var(--adm-text-muted); font-size: 12px;">Gesamt: {{ data.analytics.expos_total }}</div>
                </div>
            </div>
        </div>

    </div>

    {# Top 10 Bots #}
    <div style="background: rgba(0,0,0,0.2); border-radius: 12px; padding: 20px;">
        <h3 style="color: var(--adm-warning); margin-top: 0;">🏆 Top 10 Aktivste Bots (24h)</h3>
        <table style="width: 100%; border-collapse: collapse;">
            <thead>
                <tr style="border-bottom: 1px solid var(--adm-border); color: var(--adm-text-muted);">
                    <th style="padding: 10px; text-align: left;">Rang</th>
                    <th style="padding: 10px; text-align: left;">Bot Name</th>
                    <th style="padding: 10px; text-align: right;">Aktionen</th>
                    <th style="padding: 10px; text-align: right;">Ress gewonnen</th>
                    <th style="padding: 10px; text-align: right;">Flotten</th>
                </tr>
            </thead>
            <tbody>
                {% for bot in data.analytics.top_bots %}
                <tr style="border-bottom: 1px solid rgba(255,255,255,0.03);">
                    <td style="padding: 10px;">
                        {% if loop.index == 1 %}🥇
                        {% elseif loop.index == 2 %}🥈
                        {% elseif loop.index == 3 %}🥉
                        {% else %}{{ loop.index }}
                        {% endif %}
                    </td>
                    <td style="padding: 10px; color: var(--adm-accent);">{{ bot.username }}</td>
                    <td style="padding: 10px; text-align: right; font-weight: bold;">{{ bot.actions|number_format }}</td>
                    <td style="padding: 10px; text-align: right;">{{ bot.resources|number_format }}</td>
                    <td style="padding: 10px; text-align: right;">{{ bot.fleets|number_format }}</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js"></script>
<script>
// Aktivitäts-Heatmap
const heatmapCtx = document.getElementById('activityHeatmap');
if (heatmapCtx) {
    new Chart(heatmapCtx, {
        type: 'bar',
        data: {
            labels: [
                '00:00', '01:00', '02:00', '03:00', '04:00', '05:00',
                '06:00', '07:00', '08:00', '09:00', '10:00', '11:00',
                '12:00', '13:00', '14:00', '15:00', '16:00', '17:00',
                '18:00', '19:00', '20:00', '21:00', '22:00', '23:00'
            ],
            datasets: [{
                label: 'Bot-Aktionen',
                data: {{ data.analytics.hourly_activity|json_encode|raw }},
                backgroundColor: function(context) {
                    const value = context.parsed.y;
                    const max = Math.max(...context.dataset.data);
                    const intensity = value / max;
                    return `rgba(56, 189, 248, ${0.3 + intensity * 0.7})`;
                },
                borderColor: 'rgba(56, 189, 248, 1)',
                borderWidth: 1
            }]
        },
        options: {
            responsive: true,
            maintainAspectRatio: true,
            scales: {
                y: {
                    beginAtZero: true,
                    grid: { color: 'rgba(255,255,255,0.05)' },
                    ticks: { color: 'rgba(255,255,255,0.6)' }
                },
                x: {
                    grid: { display: false },
                    ticks: { color: 'rgba(255,255,255,0.6)' }
                }
            },
            plugins: {
                legend: { display: false }
            }
        }
    });
}

// Ressourcen-Flow Chart
const resourcesCtx = document.getElementById('resourcesChart');
if (resourcesCtx) {
    new Chart(resourcesCtx, {
        type: 'line',
        data: {
            labels: {{ data.analytics.time_labels|json_encode|raw }},
            datasets: [
                {
                    label: 'Metall',
                    data: {{ data.analytics.metal_flow|json_encode|raw }},
                    borderColor: 'rgb(156, 163, 175)',
                    backgroundColor: 'rgba(156, 163, 175, 0.1)',
                    tension: 0.4
                },
                {
                    label: 'Kristall',
                    data: {{ data.analytics.crystal_flow|json_encode|raw }},
                    borderColor: 'rgb(96, 165, 250)',
                    backgroundColor: 'rgba(96, 165, 250, 0.1)',
                    tension: 0.4
                },
                {
                    label: 'Deuterium',
                    data: {{ data.analytics.deut_flow|json_encode|raw }},
                    borderColor: 'rgb(34, 197, 94)',
                    backgroundColor: 'rgba(34, 197, 94, 0.1)',
                    tension: 0.4
                }
            ]
        },
        options: {
            responsive: true,
            maintainAspectRatio: true,
            scales: {
                y: {
                    beginAtZero: true,
                    grid: { color: 'rgba(255,255,255,0.05)' },
                    ticks: { 
                        color: 'rgba(255,255,255,0.6)',
                        callback: function(value) {
                            return value >= 1000000 ? (value/1000000).toFixed(1) + 'M' : 
                                   value >= 1000 ? (value/1000).toFixed(0) + 'k' : value;
                        }
                    }
                },
                x: {
                    grid: { display: false },
                    ticks: { color: 'rgba(255,255,255,0.6)' }
                }
            },
            plugins: {
                legend: {
                    labels: { color: 'rgba(255,255,255,0.8)' }
                }
            }
        }
    });
}
</script>
{% endif %}

{# ==================== DEV TOOLS TAB ==================== #}
{% if mode == 'dev_tools' %}
<div class="report-card">
    <div class="report-card-title">🛠️ Entwickler-Tools (Gefährlich!)</div>

    <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 20px;">
        
        {# Quick Boost #}
        <div class="dev-tool-box">
            <h4>⚡ Quick Boost für alle Bots</h4>
            <form method="post" action="admin.php?page=EditBots&mode=dev_tools" onsubmit="return confirm('Alle Bots auf diese Level setzen?');">
                <input type="hidden" name="action" value="quick_boost">
                <div class="form-group">
                    <label>Minen-Level (1/2/3)</label>
                    <input type="number" name="mine_level" value="10" min="0" max="50" class="glass-input">
                </div>
                <div class="form-group">
                    <label>Kraftwerk-Level (4/12)</label>
                    <input type="number" name="power_level" value="10" min="0" max="50" class="glass-input">
                </div>
                <div class="form-group">
                    <label>Astrophysik-Level (124)</label>
                    <input type="number" name="astro_level" value="5" min="0" max="25" class="glass-input">
                </div>
                <button type="submit" class="btn-save">🚀 Alle Bots boosten</button>
            </form>
        </div>

        {# Resource Injection #}
        <div class="dev-tool-box">
            <h4>💎 Ressourcen-Injektion</h4>
            <form method="post" action="admin.php?page=EditBots&mode=dev_tools">
                <input type="hidden" name="action" value="inject_resources">
                <div class="form-group">
                    <label>Metall</label>
                    <input type="number" name="inject_metal" value="1000000" class="glass-input">
                </div>
                <div class="form-group">
                    <label>Kristall</label>
                    <input type="number" name="inject_crystal" value="500000" class="glass-input">
                </div>
                <div class="form-group">
                    <label>Deuterium</label>
                    <input type="number" name="inject_deuterium" value="250000" class="glass-input">
                </div>
                <button type="submit" class="btn-save">💰 Ressourcen injizieren</button>
            </form>
        </div>

        {# Fleet Spawn #}
        <div class="dev-tool-box">
            <h4>🛸 Instant Flotten-Spawn</h4>
            <form method="post" action="admin.php?page=EditBots&mode=dev_tools">
                <input type="hidden" name="action" value="spawn_fleet">
                <div class="form-group">
                    <label>Kleiner Transporter (202)</label>
                    <input type="number" name="ship_202" value="100" class="glass-input">
                </div>
                <div class="form-group">
                    <label>Großer Transporter (203)</label>
                    <input type="number" name="ship_203" value="50" class="glass-input">
                </div>
                <div class="form-group">
                    <label>Recycler (209)</label>
                    <input type="number" name="ship_209" value="50" class="glass-input">
                </div>
                <div class="form-group">
                    <label>Spionagesonde (210)</label>
                    <input type="number" name="ship_210" value="50" class="glass-input">
                </div>
                <div class="form-group">
                    <label>Leichter Jäger (204)</label>
                    <input type="number" name="ship_204" value="100" class="glass-input">
                </div>
                <button type="submit" class="btn-save">🛸 Flotte auf alle Bots spawnen</button>
            </form>
        </div>

        {# Reset Bots #}
        <div class="dev-tool-box" style="border: 2px solid var(--adm-danger);">
            <h4 style="color: var(--adm-danger);">⚠️ Gefahrenzone</h4>
            <form method="post" action="admin.php?page=EditBots&mode=dev_tools" onsubmit="return confirm('ACHTUNG: Alle Bots werden gelöscht und neu erstellt!');">
                <input type="hidden" name="action" value="reset_all_bots">
                <p style="color: var(--adm-text-muted); font-size: 12px;">Löscht ALLE Bots und erstellt sie komplett neu.</p>
                <button type="submit" style="background: var(--adm-danger); border: none; padding: 10px 20px; border-radius: 6px; color: white; font-weight: bold; cursor: pointer;">🔥 Alle Bots zurücksetzen</button>
            </form>

            <hr style="border-color: rgba(255,255,255,0.1); margin: 15px 0;">

            <form method="post" action="admin.php?page=EditBots&mode=dev_tools" onsubmit="return confirm('Alle Bot next_fleet_action auf JETZT setzen?');">
                <input type="hidden" name="action" value="trigger_all_now">
                <p style="color: var(--adm-text-muted); font-size: 12px;">Alle Bots sofort aktivieren (nächster Tick = jetzt)</p>
                <button type="submit" class="btn-save">⚡ Alle Bots JETZT triggern</button>
            </form>
        </div>

    </div>
</div>
{% endif %}

{# ==================== ORIGINAL TABS (unchanged) ==================== #}
{% if mode == 'default' %}
{# ... original Contingents code ... #}
<div class="report-card">
    <div class="report-card-title">Ressourcen & Flotten Kontingente</div>
    <div style="overflow-x: auto;">
        <table style="width: 100%; border-collapse: collapse;">
            <thead>
                <tr style="border-bottom: 1px solid var(--adm-border); color: var(--adm-text-muted); text-align: left;">
                    <th style="padding: 12px;">Bot Type</th>
                    <th style="padding: 12px;">Monthly Ress Points</th>
                    <th style="padding: 12px;">Used</th>
                    <th style="padding: 12px;">Ships Contingent</th>
                    <th style="padding: 12px;">Used</th>
                    <th style="padding: 12px;">Full Contingent</th>
                    <th style="padding: 12px;">Count</th>
                </tr>
            </thead>
            <tbody>
                {% for row in data.contingents %}
                <tr style="border-bottom: 1px solid rgba(255,255,255,0.03);">
                    <td style="padding: 12px; font-weight: bold; color: var(--adm-accent);">{{ row.name }}</td>
                    <td style="padding: 12px;">
                        <input type="number" value="{{ row.ress_contingent }}"
                               onchange="updateContingent({{ row.id }}, 0, this.value, 'ress_contingent')"
                               class="glass-input-sm">
                    </td>
                    <td style="padding: 12px;">
                        <span class="{% if row.ress_contingent_used > row.ress_contingent %}text-danger{% else %}text-success{% endif %}">
                            {{ row.ress_contingent_used|number_format }}
                        </span>
                    </td>
                    <td style="padding: 12px;">
                        <input type="number" value="{{ row.ress_ships_contingent }}"
                               onchange="updateContingent({{ row.id }}, 0, this.value, 'ress_ships_contingent')"
                               class="glass-input-sm">
                    </td>
                    <td style="padding: 12px;">
                        <span class="{% if row.ress_ships_contingent_used > row.ress_ships_contingent %}text-danger{% else %}text-success{% endif %}">
                            {{ row.ress_ships_contingent_used|number_format }}
                        </span>
                    </td>
                    <td style="padding: 12px;">
                        <input type="number" value="{{ row.full_contingent }}"
                               onchange="updateContingent({{ row.id }}, 0, this.value, 'full_contingent')"
                               class="glass-input-sm">
                    </td>
                    <td style="padding: 12px;">{{ row.number_of_bots }}</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</div>
{% endif %}

{% if mode == 'show_all_bots' %}
<div class="report-card">
    <div class="report-card-title">Aktive Flottenbewegungen</div>
    <div style="overflow-x: auto;">
        <table id="botShipTable" style="width: 100%; border-collapse: collapse;">
            <thead>
                <tr style="border-bottom: 1px solid var(--adm-border); color: var(--adm-text-muted); text-align: left;">
                    <th onclick="sortTable(0, 'botShipTable')" style="padding: 12px; cursor: pointer;">Name &#8597;</th>
                    <th onclick="sortTable(1, 'botShipTable')" style="padding: 12px; cursor: pointer;">Type &#8597;</th>
                    <th onclick="sortTable(2, 'botShipTable')" style="padding: 12px; cursor: pointer;">Total Ships &#8597;</th>
                    <th style="padding: 12px;">Next Action</th>
                </tr>
            </thead>
            <tbody>
                {% for row in data.botList %}
                <tr style="border-bottom: 1px solid rgba(255,255,255,0.03);">
                    <td style="padding: 12px; color: var(--adm-accent);">{{ row.username }}</td>
                    <td style="padding: 12px;">{{ row.bot_type }}</td>
                    <td style="padding: 12px;">{{ row.total_ships|number_format }}</td>
                    <td style="padding: 12px;">{{ row.next_fleet_action|date('d.m H:i:s') }}</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</div>
{% endif %}

{% if mode == 'show_edit_bot_types' %}
<div class="report-card">
    <div class="report-card-title">Strategien Konfiguration (PVE + PVP steuerbar)</div>

    <div style="display: flex; gap: 10px; flex-wrap: wrap; margin-bottom: 20px;">
        {% for type in data.botTypes %}
        <button type="button"
                onclick="$('.type-config-form').hide(); $('#type_{{ type.id }}').fadeIn();"
                style="background: var(--adm-bg-secondary); border: 1px solid var(--adm-border); color: var(--adm-text); padding: 8px 16px; border-radius: 4px; cursor: pointer; transition: 0.2s;">
            {{ type.name }}
        </button>
        {% endfor %}
    </div>

    {% for type in data.botTypes %}
    <form method="post" action="admin.php?page=EditBots&mode=show_edit_bot_types" id="type_{{ type.id }}" class="type-config-form" style="display: {% if loop.first %}block{% else %}none{% endif %};">
        <input type="hidden" name="save_bot_types" value="1">
        <input type="hidden" name="id" value="{{ type.id }}">

        <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(300px,1fr));gap:20px;">

            <div style="background: rgba(255,255,255,0.02); padding: 15px; border-radius: 8px; border: 1px solid var(--adm-border);">
                <h4 style="color: var(--adm-accent); margin-top: 0; margin-bottom: 15px;">Basic</h4>

                <div style="margin-bottom: 15px;">
                    <label style="display:block;font-size:12px;color:var(--adm-text-muted);margin-bottom:5px;">Strategy Name</label>
                    <input type="text" name="name" value="{{ type.name }}" class="glass-input">
                </div>

                <div style="display:flex; gap:10px; flex-wrap:wrap;">
                    <label class="chk">
                        <input type="checkbox" name="enabled" {% if type.enabled %}checked{% endif %}>
                        Enabled
                    </label>
                    <label class="chk">
                        <input type="checkbox" name="pve_enabled" {% if type.pve_enabled %}checked{% endif %}>
                        PVE (Build/Exped/Recycle)
                    </label>
                    <label class="chk">
                        <input type="checkbox" name="pvp_enabled" {% if type.pvp_enabled %}checked{% endif %}>
                        PVP (Raids)
                    </label>
                </div>

                <hr style="border-color: rgba(255,255,255,0.06); margin: 15px 0;">

                <div style="display:flex; gap:10px; flex-wrap:wrap;">
                    <label class="chk"><input type="checkbox" name="can_build" {% if type.can_build %}checked{% endif %}> Build</label>
                    <label class="chk"><input type="checkbox" name="can_research" {% if type.can_research %}checked{% endif %}> Research</label>
                    <label class="chk"><input type="checkbox" name="can_shipyard" {% if type.can_shipyard %}checked{% endif %}> Shipyard</label>
                    <label class="chk"><input type="checkbox" name="can_expedition" {% if type.can_expedition %}checked{% endif %}> Expedition</label>
                    <label class="chk"><input type="checkbox" name="can_recycle" {% if type.can_recycle %}checked{% endif %}> Recycle</label>
                    <label class="chk"><input type="checkbox" name="can_raid" {% if type.can_raid %}checked{% endif %}> Raid</label>
                </div>
            </div>

            <div style="background: rgba(255,255,255,0.02); padding: 15px; border-radius: 8px; border: 1px solid var(--adm-border);">
                <h4 style="color: var(--adm-neon-green); margin-top: 0; margin-bottom: 15px;">Resource Valuation (0.0 - 1.0)</h4>

                <div style="margin-bottom: 10px;">
                    <label style="display:block;font-size:12px;color:var(--adm-text-muted);">Metal Value</label>
                    <input type="text" name="ress_value_metal" value="{{ type.ress_value_metal }}" class="glass-input">
                </div>
                <div style="margin-bottom: 10px;">
                    <label style="display:block;font-size:12px;color:var(--adm-text-muted);">Crystal Value</label>
                    <input type="text" name="ress_value_crystal" value="{{ type.ress_value_crystal }}" class="glass-input">
                </div>
                <div style="margin-bottom: 10px;">
                    <label style="display:block;font-size:12px;color:var(--adm-text-muted);">Deuterium Value</label>
                    <input type="text" name="ress_value_deuterium" value="{{ type.ress_value_deuterium }}" class="glass-input">
                </div>
            </div>

            <div style="grid-column: 1 / -1; background: rgba(255,255,255,0.02); padding: 15px; border-radius: 8px; border: 1px solid var(--adm-border);">
                <h4 style="color: var(--adm-neon-purple); margin-top: 0; margin-bottom: 15px;">Tick Limits</h4>

                <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(200px,1fr));gap:15px;">
                    <div>
                        <label style="font-size:12px;color:var(--adm-text-muted);">Max Actions / Tick</label>
                        <input type="number" name="max_actions_per_tick" value="{{ type.max_actions_per_tick|default(2) }}" class="glass-input">
                    </div>
                    <div>
                        <label style="font-size:12px;color:var(--adm-text-muted);">Max Build Queue Fill</label>
                        <input type="number" name="max_build_queue_fill" value="{{ type.max_build_queue_fill|default(1) }}" class="glass-input">
                    </div>
                    <div>
                        <label style="font-size:12px;color:var(--adm-text-muted);">Max Research Queue Fill</label>
                        <input type="number" name="max_research_queue_fill" value="{{ type.max_research_queue_fill|default(1) }}" class="glass-input">
                    </div>
                    <div>
                        <label style="font-size:12px;color:var(--adm-text-muted);">Max Shipyard Queue Fill</label>
                        <input type="number" name="max_shipyard_queue_fill" value="{{ type.max_shipyard_queue_fill|default(1) }}" class="glass-input">
                    </div>
                </div>
            </div>

            <div style="grid-column: 1 / -1; background: rgba(255,255,255,0.02); padding: 15px; border-radius: 8px; border: 1px solid var(--adm-border);">
                <h4 style="color: var(--adm-warning); margin-top: 0; margin-bottom: 15px;">Fleet Timing (Seconds)</h4>

                <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(200px,1fr));gap:15px;">
                    <div>
                        <label style="font-size:12px;color:var(--adm-text-muted);">Min Fleet Time (Space)</label>
                        <input type="number" name="min_fleet_seconds_in_space" value="{{ type.min_fleet_seconds_in_space }}" class="glass-input">
                    </div>
                    <div>
                        <label style="font-size:12px;color:var(--adm-text-muted);">Max Fleet Time (Space)</label>
                        <input type="number" name="max_fleet_seconds_in_space" value="{{ type.max_fleet_seconds_in_space }}" class="glass-input">
                    </div>
                    <div>
                        <label style="font-size:12px;color:var(--adm-text-muted);">Min Fleet Time (Planet)</label>
                        <input type="number" name="min_fleet_seconds_on_planet" value="{{ type.min_fleet_seconds_on_planet }}" class="glass-input">
                    </div>
                    <div>
                        <label style="font-size:12px;color:var(--adm-text-muted);">Max Fleet Time (Planet)</label>
                        <input type="number" name="max_fleet_seconds_on_planet" value="{{ type.max_fleet_seconds_on_planet }}" class="glass-input">
                    </div>
                </div>
            </div>

            <div style="grid-column: 1 / -1; background: rgba(255,255,255,0.02); padding: 15px; border-radius: 8px; border: 1px solid var(--adm-border);">
                <h4 style="color: var(--adm-danger); margin-top: 0; margin-bottom: 15px;">PVP Raid Rules</h4>

                <div style="display:flex; gap:10px; flex-wrap:wrap; margin-bottom: 15px;">
                    <label class="chk">
                        <input type="checkbox" name="raid_inactive_only" {% if type.raid_inactive_only %}checked{% endif %}>
                        Inactive only (7d)
                    </label>
                    <label class="chk">
                        <input type="checkbox" name="raid_allow_same_ally" {% if type.raid_allow_same_ally %}checked{% endif %}>
                        Allow same alliance
                    </label>
                </div>

                <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(200px,1fr));gap:15px;">
                    <div>
                        <label style="font-size:12px;color:var(--adm-text-muted);">Min Metal on target</label>
                        <input type="number" name="raid_min_cargo_metal" value="{{ type.raid_min_cargo_metal|default(20000) }}" class="glass-input">
                    </div>
                    <div>
                        <label style="font-size:12px;color:var(--adm-text-muted);">Min Crystal on target</label>
                        <input type="number" name="raid_min_cargo_crystal" value="{{ type.raid_min_cargo_crystal|default(10000) }}" class="glass-input">
                    </div>
                    <div>
                        <label style="font-size:12px;color:var(--adm-text-muted);">Min Gain (M+C)</label>
                        <input type="number" name="raid_min_gain" value="{{ type.raid_min_gain|default(50000) }}" class="glass-input">
                    </div>
                    <div>
                        <label style="font-size:12px;color:var(--adm-text-muted);">Max Flight Seconds</label>
                        <input type="number" name="raid_max_flight_seconds" value="{{ type.raid_max_flight_seconds|default(14400) }}" class="glass-input">
                    </div>
                    <div>
                        <label style="font-size:12px;color:var(--adm-text-muted);">Max Rank Diff (points)</label>
                        <input type="number" name="raid_max_rank_diff" value="{{ type.raid_max_rank_diff|default(250) }}" class="glass-input">
                    </div>
                </div>
            </div>

            <div style="grid-column: 1 / -1; background: rgba(255,255,255,0.02); padding: 15px; border-radius: 8px; border: 1px solid var(--adm-border);">
                <h4 style="color: var(--adm-accent); margin-top: 0; margin-bottom: 10px;">Fleet Preset (serialized)</h4>
                <p style="color:var(--adm-text-muted); margin-top:0; font-size:12px;">
                    Beispiel: <code>a:3:{i:202;i:50;i:210;i:10;i:209;i:10;}</code> (ShipID =&gt; Amount)
                </p>
                <textarea name="ships_array" class="glass-input" rows="3" style="font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;">{{ type.ships_array|default('') }}</textarea>
            </div>

        </div>

        <div style="margin-top: 20px; text-align: right;">
            <button type="submit" class="btn-save">Save Configuration</button>
        </div>
    </form>
    {% endfor %}
</div>
{% endif %}

{% if mode == 'show_edit_bot_users' %}
<div class="report-card">
    <div class="report-card-title">Aktive Bot Accounts</div>
    <div style="overflow-x: auto;">
        <table style="width: 100%; border-collapse: collapse;">
            <thead>
                <tr style="border-bottom: 1px solid var(--adm-border); color: var(--adm-text-muted); text-align: left;">
                    <th style="padding: 12px;">ID</th>
                    <th style="padding: 12px;">Username</th>
                    <th style="padding: 12px;">Strategy</th>
                    <th style="padding: 12px;">Alliance</th>
                    <th style="padding: 12px;">Location</th>
                    <th style="padding: 12px;">Last Activity</th>
                    <th style="padding: 12px;">Action</th>
                </tr>
            </thead>
            <tbody>
                {% for bot in data.activeBots %}
                <tr style="border-bottom: 1px solid rgba(255,255,255,0.03);">
                    <td style="padding: 12px;">{{ bot.id }}</td>
                    <td style="padding: 12px; color: var(--adm-accent);">{{ bot.username }}</td>
                    <td style="padding: 12px;">{{ bot.bot_type_name }}</td>
                    <td style="padding: 12px;">{{ bot.ally_name|default('-') }}</td>
                    <td style="padding: 12px;">[{{ bot.galaxy }}:{{ bot.system }}:{{ bot.planet }}]</td>
                    <td style="padding: 12px;">{{ bot.onlinetime|date('d.m.Y H:i') }}</td>
                    <td style="padding: 12px;">
                        <form method="post" action="admin.php?page=EditBots&mode=delete_bot" onsubmit="return confirm('Account #{{ bot.id }} wirklich löschen?');" style="margin:0;">
                            <input type="hidden" name="bot_id" value="{{ bot.id }}">
                            <button type="submit" style="background: var(--adm-danger); border: none; padding: 6px 12px; border-radius: 4px; color: white; cursor: pointer; font-size: 11px;">
                                LÖSCHEN
                            </button>
                        </form>
                    </td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</div>
{% endif %}

{% if mode == 'show_edit_bot_planets' %}
<div class="report-card">
    <div class="report-card-title">Bot Planet Settings (Massenbearbeitung)</div>

    <div style="background: rgba(56, 189, 248, 0.05); border: 1px solid var(--adm-border); padding: 15px; border-radius: 8px; margin-bottom: 20px; display: flex; align-items: center; gap: 15px; flex-wrap: wrap;">
        <span style="font-weight: bold; color: var(--adm-text);"><span style="color:var(--adm-warning)">⚠</span> Mass Action:</span>
        <form method="post" action="admin.php?page=EditBots&mode=show_edit_bot_planets" style="display: flex; gap: 10px; margin: 0; flex-wrap: wrap;">
            <input type="number" name="elementid" placeholder="Element ID" class="glass-input" style="width: 120px;">
            <input type="number" name="elemval" placeholder="Level/Amount" class="glass-input" style="width: 120px;">
            <button type="submit" class="btn-save">Auf ALLE Planeten anwenden</button>
        </form>
    </div>

    <div style="overflow-x: auto; max-height: 600px;">
        <table style="width: 100%; border-collapse: collapse; font-size: 12px;">
            <thead>
                <tr style="border-bottom: 1px solid var(--adm-border); color: var(--adm-text-muted); text-align: left; position: sticky; top: 0; background: var(--adm-bg-card); z-index: 10;">
                    <th style="padding: 10px;">PID</th>
                    <th style="padding: 10px;">Name</th>
                    <th style="padding: 10px;">Owner ID</th>
                    <th style="padding: 10px;">Mines (M/C/D)</th>
                    <th style="padding: 10px;">Solar</th>
                    <th style="padding: 10px;">Fusion</th>
                    <th style="padding: 10px;">Storage</th>
                </tr>
            </thead>
            <tbody>
                {% for planet in data.botPlanets %}
                <tr style="border-bottom: 1px solid rgba(255,255,255,0.03);">
                    <td style="padding: 8px;">{{ planet.id }}</td>
                    <td style="padding: 8px; color: var(--adm-accent);">{{ planet.name }}</td>
                    <td style="padding: 8px;">{{ planet.id_owner }}</td>
                    <td style="padding: 8px;">{{ planet.1|default(0) }} / {{ planet.2|default(0) }} / {{ planet.3|default(0) }}</td>
                    <td style="padding: 8px;">{{ planet.4|default(0) }}</td>
                    <td style="padding: 8px;">{{ planet.12|default(0) }}</td>
                    <td style="padding: 8px;">{{ planet.22|default(0) }} / {{ planet.23|default(0) }} / {{ planet.24|default(0) }}</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</div>
{% endif %}

{% if mode == 'show_create_bot' or mode == 'create_bot' %}
<div style="display: flex; justify-content: center; margin-top: 40px;">
    <div class="report-card" style="width: 100%; max-width: 500px; padding: 30px;">
        <div style="text-align: center; margin-bottom: 30px;">
            <div style="font-size: 40px; margin-bottom: 10px;">&#129302;</div>
            <h2 style="color: var(--adm-text); margin: 0;">Bot Generator</h2>
            <p style="color: var(--adm-text-muted);">Erstellt zufällige Bots basierend auf den Strategien</p>
        </div>

        <form method="post" action="admin.php?page=EditBots&mode=create_bot">
            <div style="margin-bottom: 20px;">
                <label style="display: block; margin-bottom: 8px; color: var(--adm-text-muted); font-weight: 500;">Anzahl der Bots</label>
                <input type="number" name="number_of_bots" value="1" min="1" class="glass-input" style="font-size: 16px;">
            </div>

            <div style="margin-bottom: 30px;">
                <label style="display: block; margin-bottom: 8px; color: var(--adm-text-muted); font-weight: 500;">Planeten pro Bot</label>
                <input type="number" name="number_of_planets" value="1" min="1" max="15" class="glass-input" style="font-size: 16px;">
            </div>

            <button type="submit" style="width: 100%; padding: 14px; background: linear-gradient(135deg, var(--adm-neon-green), var(--adm-success)); border: none; border-radius: 8px; color: #000; font-weight: bold; font-size: 16px; cursor: pointer; transition: transform 0.2s;">
                Bots Generieren Starten
            </button>
        </form>
    </div>
</div>
{% endif %}



<style>
/* ==================== TAB BUTTONS ==================== */
.period-tabs {
    display: flex;
    gap: 8px;
    flex-wrap: wrap;
    border-bottom: 1px solid rgba(255,255,255,0.1);
    padding-bottom: 10px;
}

.period-tab {
    background: rgba(255,255,255,0.03);
    border: 1px solid rgba(255,255,255,0.1);
    color: rgba(255,255,255,0.6);
    padding: 8px 16px;
    border-radius: 6px;
    text-decoration: none;
    font-size: 13px;
    font-weight: 500;
    transition: all 0.2s ease;
    white-space: nowrap;
    display: inline-block;
}

.period-tab:hover {
    background: rgba(56, 189, 248, 0.1);
    border-color: rgba(56, 189, 248, 0.3);
    color: #38bdf8;
    transform: translateY(-1px);
}

.period-tab.active {
    background: linear-gradient(135deg, rgba(56, 189, 248, 0.2), rgba(99, 102, 241, 0.2));
    border-color: #38bdf8;
    color: #38bdf8;
    box-shadow: 0 0 15px rgba(56, 189, 248, 0.3);
}

/* ==================== FORMS & INPUTS ==================== */
.glass-input {
    width: 100%;
    background: var(--adm-bg);
    border: 1px solid var(--adm-border);
    color: #fff;
    padding: 10px;
    border-radius: 4px;
    outline: none;
    transition: 0.3s;
}
.glass-input:focus {
    border-color: var(--adm-accent);
    box-shadow: 0 0 10px var(--adm-accent-glow);
}
.glass-input-sm {
    background: var(--adm-bg-secondary);
    border: 1px solid var(--adm-border);
    color: #fff;
    padding: 4px 8px;
    border-radius: 4px;
    width: 100px;
    outline: none;
}
.btn-save {
    background: linear-gradient(135deg, var(--adm-accent), #0ea5e9);
    border: none;
    padding: 10px 20px;
    border-radius: 6px;
    color: #000;
    font-weight: bold;
    cursor: pointer;
}
.chk {
    display:flex;
    gap:8px;
    align-items:center;
    font-size:12px;
    color:var(--adm-text);
    background: rgba(255,255,255,0.03);
    border:1px solid rgba(255,255,255,0.06);
    padding:6px 10px;
    border-radius:999px;
}
.chk input { transform: translateY(1px); }

.config-section {
    background: rgba(255,255,255,0.02);
    padding: 20px;
    border-radius: 8px;
    border: 1px solid var(--adm-border);
}
.config-section h4 {
    margin-top: 0;
    margin-bottom: 15px;
    color: var(--adm-text);
}
.form-group {
    margin-bottom: 15px;
}
.form-group label {
    display: block;
    font-size: 12px;
    color: var(--adm-text-muted);
    margin-bottom: 5px;
    font-weight: 500;
}
.form-group small {
    display: block;
    font-size: 11px;
    color: var(--adm-text-muted);
    margin-top: 3px;
}
.checkbox-group {
    display: flex;
    flex-direction: column;
    gap: 8px;
}
.range-slider {
    width: 100%;
    height: 6px;
    border-radius: 5px;
    background: rgba(255,255,255,0.1);
    outline: none;
}
.range-slider::-webkit-slider-thumb {
    -webkit-appearance: none;
    appearance: none;
    width: 18px;
    height: 18px;
    border-radius: 50%;
    background: var(--adm-accent);
    cursor: pointer;
}
output {
    display: inline-block;
    margin-left: 10px;
    color: var(--adm-accent);
    font-weight: bold;
}

.type-select-btn {
    background: var(--adm-bg-secondary);
    border: 1px solid var(--adm-border);
    color: var(--adm-text);
    padding: 8px 16px;
    border-radius: 4px;
    cursor: pointer;
    transition: 0.2s;
}
.type-select-btn:hover {
    background: var(--adm-accent);
    color: #000;
}

.stat-box {
    background: linear-gradient(135deg, rgba(14, 165, 233, 0.1), rgba(99, 102, 241, 0.1));
    border: 1px solid var(--adm-border);
    border-radius: 8px;
    padding: 15px;
    text-align: center;
}
.stat-label {
    font-size: 11px;
    color: var(--adm-text-muted);
    text-transform: uppercase;
    letter-spacing: 0.5px;
    margin-bottom: 5px;
}
.stat-value {
    font-size: 28px;
    font-weight: bold;
    color: var(--adm-accent);
}

.dev-tool-box {
    background: rgba(255,255,255,0.02);
    border: 1px solid var(--adm-border);
    border-radius: 8px;
    padding: 20px;
}
.dev-tool-box h4 {
    margin-top: 0;
    margin-bottom: 15px;
}

/* ==================== ANALYTICS STYLES ==================== */
.kpi-card {
    background: linear-gradient(135deg, rgba(56, 189, 248, 0.1), rgba(99, 102, 241, 0.1));
    border: 1px solid var(--adm-border);
    border-radius: 12px;
    padding: 20px;
    text-align: center;
    transition: transform 0.2s;
}
.kpi-card:hover {
    transform: translateY(-3px);
    box-shadow: 0 10px 30px rgba(56, 189, 248, 0.2);
}
.kpi-icon {
    font-size: 32px;
    margin-bottom: 10px;
}
.kpi-value {
    font-size: 28px;
    font-weight: bold;
    color: var(--adm-accent);
    margin-bottom: 5px;
}
.kpi-label {
    font-size: 11px;
    color: var(--adm-text-muted);
    text-transform: uppercase;
    letter-spacing: 0.5px;
}

.progress-circle {
    width: 80px;
    height: 80px;
    border-radius: 50%;
    background: conic-gradient(
        var(--adm-accent) calc(var(--percent) * 1%), 
        rgba(255,255,255,0.1) 0
    );
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
}
.progress-circle::before {
    content: '';
    width: 60px;
    height: 60px;
    border-radius: 50%;
    background: var(--adm-bg-card);
    position: absolute;
}
.progress-circle span {
    position: relative;
    z-index: 1;
    font-weight: bold;
    font-size: 16px;
    color: var(--adm-accent);
}

</style>

<script>
function updateContingent(botType, userId, value, type) {
    var xhr = new XMLHttpRequest();
    xhr.open("POST", "admin.php?page=EditBots", true);
    xhr.setRequestHeader("Content-Type", "application/x-www-form-urlencoded");
    xhr.send("bot_type=" + botType + "&userid=" + userId + "&" + type + "=" + value);
}
</script>

{% include 'overall_footer.twig' %}
