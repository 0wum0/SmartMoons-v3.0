<nav id="leftmenu" class="sidebar-nav" role="navigation" aria-label="Hauptnavigation">

    <div class="sidebar-content">

        <!-- UEBERSICHT -->
        <div class="nav-section dock-section-overview">
            <div class="nav-section-title"><span>{{ LNG.lm_overview }}</span></div>
            <a href="game.php?page=overview"
               class="sidebar-link dock-mirrored{% if GET.page|default('overview') == 'overview' %} active{% endif %}"
               data-label="{{ LNG.lm_overview }}">
                <i class="fas fa-home"></i><span>{{ LNG.lm_overview }}</span>
            </a>
        </div>

        <!-- IMPERIUM -->
        <div class="nav-section">
            <div class="nav-section-title"><span>{{ LNG.lm_empire }}</span></div>

            {% if isModuleAvailable(constant('MODULE_BUILDING')) %}
            <a href="game.php?page=buildings"
               class="sidebar-link dock-mirrored{% if GET.page|default('') == 'buildings' %} active{% endif %}"
               data-label="{{ LNG.lm_buildings }}">
                <i class="fas fa-city"></i><span>{{ LNG.lm_buildings }}</span>
            </a>
            {% endif %}

            {% if isModuleAvailable(constant('MODULE_RESEARCH')) %}
            <a href="game.php?page=research"
               class="sidebar-link{% if GET.page|default('') == 'research' %} active{% endif %}"
               data-label="{{ LNG.lm_research }}">
                <i class="fas fa-flask"></i><span>{{ LNG.lm_research }}</span>
            </a>
            {% endif %}

            {% if isModuleAvailable(constant('MODULE_SHIPYARD_FLEET')) %}
            <a href="game.php?page=shipyard&mode=fleet"
               class="sidebar-link{% if GET.page|default('') == 'shipyard' and GET.mode|default('') == 'fleet' %} active{% endif %}"
               data-label="{{ LNG.lm_shipshard }}">
                <i class="fas fa-rocket"></i><span>{{ LNG.lm_shipshard }}</span>
            </a>
            {% endif %}

            {% if isModuleAvailable(constant('MODULE_SHIPYARD_DEFENSIVE')) %}
            <a href="game.php?page=shipyard&mode=defense"
               class="sidebar-link{% if GET.page|default('') == 'shipyard' and GET.mode|default('') == 'defense' %} active{% endif %}"
               data-label="{{ LNG.lm_defenses }}">
                <i class="fas fa-shield-alt"></i><span>{{ LNG.lm_defenses }}</span>
            </a>
            {% endif %}

            {% if isModuleAvailable(constant('MODULE_RESSOURCE_LIST')) %}
            <a href="game.php?page=resources"
               class="sidebar-link{% if GET.page|default('') == 'resources' %} active{% endif %}"
               data-label="{{ LNG.lm_resources }}">
                <i class="fas fa-industry"></i><span>{{ LNG.lm_resources }}</span>
            </a>
            {% endif %}

            {% if isModuleAvailable(constant('MODULE_OFFICIER')) or isModuleAvailable(constant('MODULE_DMEXTRAS')) %}
            <a href="game.php?page=officier"
               class="sidebar-link{% if GET.page|default('') == 'officier' %} active{% endif %}"
               data-label="{{ LNG.lm_officiers }}">
                <i class="fas fa-user-tie"></i><span>{{ LNG.lm_officiers }}</span>
            </a>
            {% endif %}
        </div>

        <!-- FLOTTE & KARTE -->
        <div class="nav-section">
            <div class="nav-section-title"><span>{{ LNG.lm_navigation }}</span></div>

            {% if isModuleAvailable(constant('MODULE_FLEET_TABLE')) %}
            <a href="game.php?page=fleetTable"
               class="sidebar-link dock-mirrored{% if GET.page|default('') == 'fleetTable' %} active{% endif %}"
               data-label="{{ LNG.lm_fleet }}">
                <i class="fas fa-space-shuttle"></i><span>{{ LNG.lm_fleet }}</span>
            </a>
            {% endif %}

            {% if isModuleAvailable(constant('MODULE_GALAXY')) %}
            <a href="game.php?page=galaxy"
               class="sidebar-link dock-mirrored{% if GET.page|default('') == 'galaxy' %} active{% endif %}"
               data-label="{{ LNG.lm_galaxy }}">
                <i class="fas fa-globe"></i><span>{{ LNG.lm_galaxy }}</span>
            </a>
            {% endif %}

            {% if isModuleAvailable(constant('MODULE_FLEET_TRADER')) %}
            <a href="game.php?page=fleetDealer"
               class="sidebar-link{% if GET.page|default('') == 'fleetDealer' %} active{% endif %}"
               data-label="{{ LNG.lm_fleettrader }}">
                <i class="fas fa-recycle"></i><span>{{ LNG.lm_fleettrader }}</span>
            </a>
            {% endif %}

            {% if isModuleAvailable(constant('MODULE_TRADER')) %}
            <a href="game.php?page=trader"
               class="sidebar-link{% if GET.page|default('') == 'trader' %} active{% endif %}"
               data-label="{{ LNG.lm_trader }}">
                <i class="fas fa-exchange-alt"></i><span>{{ LNG.lm_trader }}</span>
            </a>
            {% endif %}
        </div>

        <!-- GEMEINSCHAFT -->
        <div class="nav-section">
            <div class="nav-section-title"><span>{{ LNG.lm_community|default('Community') }}</span></div>

            {% if isModuleAvailable(constant('MODULE_MESSAGES')) %}
            <a href="game.php?page=messages"
               class="sidebar-link{% if GET.page|default('') == 'messages' %} active{% endif %}"
               data-label="{{ LNG.lm_messages }}">
                <i class="fas fa-envelope"></i><span>{{ LNG.lm_messages }}</span>
                {% if new_message is defined and new_message > 0 %}
                <span class="nav-badge">{{ new_message }}</span>
                {% endif %}
            </a>
            {% endif %}

            {% if isModuleAvailable(constant('MODULE_ALLIANCE')) %}
            <a href="game.php?page=alliance"
               class="sidebar-link{% if GET.page|default('') == 'alliance' %} active{% endif %}"
               data-label="{{ LNG.lm_alliance }}">
                <i class="fas fa-users"></i><span>{{ LNG.lm_alliance }}</span>
            </a>
            {% endif %}

            {% if isModuleAvailable(constant('MODULE_FORUM')) %}
            <a href="game.php?page=forum"
               class="sidebar-link{% if GET.page|default('') == 'forum' %} active{% endif %}"
               data-label="Forum">
                <i class="fas fa-comments"></i><span>Forum</span>
            </a>
            {% endif %}

            {% if isModuleAvailable(constant('MODULE_SUPPORT')) %}
            <a href="game.php?page=ticket"
               class="sidebar-link{% if GET.page|default('') == 'ticket' %} active{% endif %}"
               data-label="{{ LNG.lm_support }}">
                <i class="fas fa-life-ring"></i><span>{{ LNG.lm_support }}</span>
            </a>
            {% endif %}
        </div>

        <!-- TOOLS -->
        <div class="nav-section">
            <div class="nav-section-title"><span>{{ LNG.lm_tools|default('Tools') }}</span></div>

            {% if isModuleAvailable(constant('MODULE_STATISTICS')) %}
            <a href="game.php?page=statistics"
               class="sidebar-link{% if GET.page|default('') == 'statistics' %} active{% endif %}"
               data-label="{{ LNG.lm_statistics }}">
                <i class="fas fa-chart-line"></i><span>{{ LNG.lm_statistics }}</span>
            </a>
            {% endif %}

            {% if isModuleAvailable(constant('MODULE_BATTLEHALL')) %}
            <a href="game.php?page=battleHall"
               class="sidebar-link{% if GET.page|default('') == 'battleHall' %} active{% endif %}"
               data-label="{{ LNG.lm_topkb }}">
                <i class="fas fa-trophy"></i><span>{{ LNG.lm_topkb }}</span>
            </a>
            {% endif %}

            {% if isModuleAvailable(constant('MODULE_RECORDS')) %}
            <a href="game.php?page=records"
               class="sidebar-link{% if GET.page|default('') == 'records' %} active{% endif %}"
               data-label="{{ LNG.lm_records }}">
                <i class="fas fa-medal"></i><span>{{ LNG.lm_records }}</span>
            </a>
            {% endif %}

            {% if isModuleAvailable(constant('MODULE_SIMULATOR')) %}
            <a href="game.php?page=battleSimulator"
               class="sidebar-link{% if GET.page|default('') == 'battleSimulator' %} active{% endif %}"
               data-label="{{ LNG.lm_battlesim }}">
                <i class="fas fa-chess-knight"></i><span>{{ LNG.lm_battlesim }}</span>
            </a>
            {% endif %}

            {% if isModuleAvailable(constant('MODULE_SEARCH')) %}
            <a href="game.php?page=search"
               class="sidebar-link{% if GET.page|default('') == 'search' %} active{% endif %}"
               data-label="{{ LNG.lm_search }}">
                <i class="fas fa-search"></i><span>{{ LNG.lm_search }}</span>
            </a>
            {% endif %}

            {% if isModuleAvailable(constant('MODULE_TECHTREE')) %}
            <a href="game.php?page=techtree"
               class="sidebar-link{% if GET.page|default('') == 'techtree' %} active{% endif %}"
               data-label="{{ LNG.lm_technology }}">
                <i class="fas fa-sitemap"></i><span>{{ LNG.lm_technology }}</span>
            </a>
            {% endif %}
        </div>

    </div>

    <!-- SIDEBAR FOOTER -->
    <div class="sidebar-footer">
        </div>
    <!-- Collapse Button -->
    <button class="sidebar-collapse-btn" id="sidebar-collapse-btn"
            aria-label="Sidebar einklappen">
        <i class="fas fa-chevron-left" id="sidebar-collapse-icon"></i>
        <span id="sidebar-collapse-label">Einklappen</span>
    </button>

    {{ hook('sidebar_end') }}
</nav>
